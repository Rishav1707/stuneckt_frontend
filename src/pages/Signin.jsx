/* eslint-disable react/prop-types */
import SigninComponent from "../components/Signin";

const Signin = ({ setIsLoggedIn }) => {
  return <SigninComponent setIsLoggedIn={setIsLoggedIn} />;
};

export default Signin;
